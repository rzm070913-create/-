## v2.1.9｜角色陪玩选择自动保存、UNO/斗地主角色模式修复与代码清理
- 角色陪玩勾选角色后立即保存到全局配置，不需要先点击“保存陪玩设置”；显式保存按钮继续保留。
- 新开 UNO / 斗地主时，如已有已保存陪玩角色，自动使用这套角色配置；已有进行中的存档则保持原来的模式。
- 清理角色陪玩选择器中未使用的世界书筛选临时字段与未使用函数，减少重复逻辑。

## v2.1.5｜角色气泡、全多人陪玩头像、中国象棋修复
- 棋类角色台词改为头像上方的气泡显示，同时保留底部对话输入区；UNO、斗地主和棋类陪玩角色均显示角色头像。
- 五子棋、围棋、国际象棋、中国象棋、UNO、斗地主等已接入角色陪玩时，统一使用已保存的陪玩角色与头像；角色台词允许自然挑衅、得意、安慰、鼓励和嘴硬，但不会改变游戏规则。
- 修复中国象棋进入后可能白屏的问题：修正陪玩聊天同步时的配置引用，并增强旧存档棋盘数据校验。
- 修复角色陪玩生成后的棋类气泡刷新，角色落子后会把本次台词显示到对应头像上方。
- 版本号采用三段式十进制，本版为 2.1.9。

## v2.1.4｜UNO 出牌颜色规则修复
- 修复 UNO 最关键的出牌判定 bug：数字牌不再因为上一张也是数字牌就无视颜色直接出牌。
- 数字牌只能同色或同数字；Skip / Reverse / +2 等功能牌可以同色或同功能匹配；万能牌按 UNO 规则处理。
- 角色陪玩、本地 AI、玩家手动出牌与 AI 合法行动列表统一使用同一个规则引擎。

## v2.1.2｜角色陪玩配置热更新 + 五子棋策略增强
- 五子棋、围棋、国际象棋、中国象棋全部加入“角色陪玩”模式。每局棋可使用已在“角色陪玩”中选择的第 1 名角色作为唯一 AI 对手；本地 AI / 角色陪玩 / 双人对战三种模式可切换。
- 角色棋步只能从 Silly Game 规则引擎生成的合法走法中选择；角色 AI 负责策略与短台词，非法或请求失败时自动回退本地 AI，不会污染棋盘。
- 新增棋类陪玩对话框，可直接和对手说话；附带“求放水”快捷按钮。求放水会真实降低对手下一回合的策略强度，但仍遵守游戏规则。
- 棋类对话与落子都统一使用角色陪玩 API 连接配置，并计入可调的每分钟请求限流。

## v1.10.9｜斗地主人类选牌判定修复
- 修复玩家手里有多张同点数牌时，选择非第一张牌被误判为“这手牌不合法”。
- 人类玩家现在直接对实际选中的牌重新判型并与上一手比较，不再依赖预生成组合的代表下标。
- AI/本地 AI 原有规则与存档逻辑保持不变。

## v1.10.8｜斗地主存档/后台 AI 修复
- 关闭斗地主或切换游戏后，旧的 AI 定时器与未完成请求不会继续修改或覆盖牌局存档。
- 修复快速切换“角色陪玩 / 本地 AI / 重新开始”可能留下旧计时器的问题。
- 玩家出牌非法时明确保持当前选牌，不会自动执行“不出”；新增“重选”按钮。
- 斗地主出牌继续经过统一规则引擎校验。

## v1.10.7｜斗地主视觉重制
- 斗地主牌桌整体视觉重做：标准扑克牌、三人座位、地主/回合徽章、中央出牌区、手牌叠放与移动端适配。
- 保留角色陪玩、本地 AI、叫地主、规则引擎、存档与限流逻辑。

## v1.10.4｜角色陪玩升级
- 普通角色只读取角色简介（name + description），不主动读取 personality、scenario、示例对话或整本世界书。
- 角色卡世界书可按需展开；可单独选择某个启用条目作为一个独立陪玩角色，1 个条目 = 1 个角色席位。
- 保留最多 3 个陪玩席位；UNO 使用最多 3 名角色，斗地主使用前 2 名角色参与。
- 角色陪玩 AI 请求统一增加滚动 60 秒最多 10 次、请求启动间隔至少 6 秒的限流保护，并在界面显示请求计数与倒计时。
- 新增斗地主：叫地主、三人对局、单牌/对子/三条/三带一/三带二/顺子/连对/飞机/四带二/炸弹/王炸，支持角色陪玩与本地 AI。
- 斗地主胜利可解锁农场作物“荷花”。

# Silly Game

## 游戏
角色陪玩、扫雷、2048、推箱子、数独、蜘蛛纸牌、五子棋、数字华容道、俄罗斯方块、围棋、倒水瓶、小农场、叠蛋糕、消灭星星、连连看、数方、国际象棋、中国象棋、UNO、斗地主。

## v1.9.4｜角色陪玩改用 API 连接配置
- 角色陪玩改为读取 SillyTavern 的 **API 连接配置（Connection Profiles）**，不再读取当前 RP 的 Chat Completion Preset。
- 使用 `ConnectionManagerRequestService` 直接按所选配置发请求，不切换当前 RP 的全局连接。
- 下拉项显示“配置名 · 模型”，支持刷新；RP 可以继续使用 Pro，小游戏单独使用轻量模型/中转配置。
- 角色列表默认折叠，展开后支持搜索；只显示匹配角色，降低大量角色卡时的视觉负担。
- 保留最多 3 名角色同时陪玩与未选满席位使用本地 AI 补位。

## v1.7.3
- 清理重复 CSS 规则与重复属性，保留最终级联结果。
- 新增 UNO：108 张牌、你 + 3 名本地 AI；支持数字、跳过、反转、+2、变色、+4、摸牌、喊 UNO。
- UNO 胜利解锁农场作物“苹果”。
- JS / manifest / README 版本号统一为 1.6.6。

## 更新检查
每次进入/刷新 SillyTavern 时后台检查一次 GitHub 托管版本。通过 GitHub 安装的扩展可使用更新流程。


## v1.7.3
- 新增 UNO 专属牌面与桌面视觉：彩色圆角卡牌、椭圆牌芯、可出牌高亮、牌堆与变色选择器。


## v1.7.3
- UNO 明确区分“摸牌堆”和“出牌区”。
- AI 回合间隔提高到约 1.3 秒，方便看清每次 AI 出的牌。
- 当前出牌增加短暂落牌动画与动作提示。
- 万能牌选色提示不再以大块遮罩盖住整个中心区域。


## v1.7.3 UNO
- 严格区分同色、同数字/同功能牌。
- +4 按经典规则仅在手中没有当前颜色牌时可出。
- 摸牌后只能处理这张新牌；不能出则可“过牌”。
- 颜色选择器移动到 AI 2 下方的空白区域，不遮挡牌桌。
- 不可出的手牌自动弱化并禁用点击。


## v1.7.3
- 新增三消：交换相邻软糖方块、连锁消除、30步目标挑战。
- 三消通关解锁农场作物「桃花」。


## 1.7.3
- 三消改为六种软糖造型：星星、阿尔卑斯、熊熊、心形、圆环、果冻。
- 保留原来的莫兰迪颜色系统并兼容旧存档。


## v1.8.6
- 三消改为六种独立软糖造型：糖本体直接显示，不再是“彩色方块+图标”。
- 软糖颜色直接来自方块配色，棋盘底色保持不变。

## v1.8.6
- 三消统一为 6 种固定软糖元素：每种软糖固定一种颜色，不再生成 6×5 的 30 种组合。
- 匹配逻辑按软糖元素判断，旧版存档会自动按元素恢复固定颜色。
- 新局生成时确保尽量存在可操作的交换。


## v1.8.6
- 三消新增 4 消条纹软糖、T/L 形 3×3 炸弹糖、5 消彩虹糖。
- 新增条纹/炸弹/彩虹特殊糖组合效果。
- 保持六种基础软糖固定颜色，特殊糖作为强化形态。


## v1.8.6
- 三消关卡升级为 30 关，目标分数与步数递进提高难度。
- 新增冰块、藤蔓障碍。
- 通关后直接出现“下一关”按钮，不再需要手动寻找下一关。


## v1.8.6
- 三消关卡目标得分整体提高。
- 三消加入多种棋盘挖空形状，关卡不再固定完整方形。
- 通关后显示明确的“下一关”按钮。


## v1.8.6
- 三消通关后改为覆盖整个游戏区域的通关弹窗，中央直接提供下一关按钮。


## v1.8.6
三消棋盘形状挖空改为永久洞位：糖果不会跨过洞位下落或补入，消除后只在每个连续区域内进行下落与补充。


## v1.8.6
- 三消棋盘改为透明网格底，永久挖空区域直接显示 Silly Game 弹窗背景。
- 没有可合成组合时自动洗牌并继续游戏；保留挖空形状。

## v1.8.7
- 修复数独点击「×」关闭后局面保存不够稳的问题。
- 关闭游戏、页面进入后台、刷新/关闭页面前都会主动保存当前数独。
- 数独存档仍保留在浏览器本地，不需要额外 API。



## v1.9.1｜角色陪玩增强
- 角色陪玩由“单角色”升级为“最多 3 名角色同时陪玩”，对应 UNO 三个 AI 席位。
- 角色选择改为多选；未选满的席位自动由本地 AI 补位。
- 新增“AI 生成预设”选择，可读取酒馆当前 API 类型下已经保存的 API 预设。
- 选择指定 API 预设时，陪玩生成会临时套用该预设，生成结束后自动恢复酒馆原先使用的预设，不长期改动用户当前设置。
- 当前角色仍优先使用 quiet generation，以保留当前角色与聊天上下文；其他已选角色继续按角色卡内容生成。
- UNO 的每个角色席位都会使用自己的角色身份、手牌与行动回合。

## v1.9.0｜角色陪玩
- 新增「角色陪玩」核心入口，角色玩家层与具体游戏解耦，后续斗地主、五子棋、象棋、围棋可复用。
- 首发接入 UNO：酒馆角色拥有真实手牌并参与回合。
- 当前角色优先使用 SillyTavern 的后台 quiet generation，保留角色与当前聊天上下文；从角色列表选择的其他卡使用角色卡文本进行原始生成。
- 角色只负责选择行动和简短台词，合法性、摸牌、出牌、胜负全部由 Silly Game UNO 引擎裁定。
- 角色返回非法行动时自动回退到本地 AI，不会卡死牌局。
- 首页新增「角色陪玩」入口；可选择当前角色/角色列表角色，并开关角色台词。
- UNO 角色陪玩与原有本地 AI 模式兼容，关闭窗口时仍会保存当前牌局。

## v1.9.2｜角色陪玩预设与兼容性修复
- 角色陪玩 API 预设改为读取 SillyTavern 全部“聊天补全预设”，不再跟随当前 RP 所使用的预设。
- 陪玩默认独立选择预设；RP 可以使用高阶模型，小游戏可以单独选择更轻量、更便宜的预设。
- 修复预设临时切换时给 `selectPreset()` 传入预设名称的问题，改为使用正确的 preset option value，并在生成后恢复原预设。
- UNO 角色生成改为显式 `generateRaw({ prompt })`，不再强依赖 `response_format/json_schema`，提高 OpenAI 兼容中转端的兼容性；非法 JSON/动作会重试一次，仍失败则回退本地 AI。
- 内置角色陪玩提示词增强：游戏引擎负责裁判，模型只从当前允许行动中选择。

## v1.10.4
- UNO 角色陪玩与斗地主统一使用角色陪玩选择结果：支持普通角色简介与指定世界书条目角色。
- 角色选择器改用 SillyTavern 原生 `menu_button` / `text_pole` / `checkbox_label` 视觉体系，只保留布局需要的 CSS，不再自定义黑色面板背景。
- UNO 重新开始与进入游戏时会正确保留世界书条目角色选择。

## v1.10.4 补充
- UNO 与斗地主现在完全共用“最多 3 名陪玩角色”的选择结果。
- UNO 也支持把普通角色简介或指定世界书条目作为陪玩角色，并继续遵守全局 10 次/60 秒、请求间隔 6 秒的限流。
- 角色选择区改为使用 SillyTavern 原生控件外观，不再给角色项强制套深色背景。

### v1.10.11
- 棋类角色陪玩支持在当前已选角色之间直接“换陪玩”。
- “求放水”按钮取消括号及“做梦中”文案；点击后下一次对手落子会真实采用较弱的合法策略。
- 版本号继续采用三段十进制递增，本版为 1.10.11。




Version 2.1.6: fixed Shikaku initial blank screen caused by loading clue grid from an incorrect nested property.


## v2.1.8
- 修复 UNO 颜色状态可能残留导致错误跨色出牌的问题。
- 普通彩色牌的合法性现在严格按“当前颜色 / 同数字 / 同功能”判断。
- 非法手牌不再渲染成可点击按钮，真正从交互层阻断误出牌；底层出牌函数仍保留二次规则校验。
- 修复万能牌与 +4 的选色：打出后当前颜色严格采用玩家/AI选择的颜色。
- 旧 UNO 存档进入游戏时会自动用弃牌顶同步当前颜色。

## v2.1.7
- 重做主题可读性：默认跟随酒馆主题，但使用高对比度安全表面。
- 新增主题模式：跟随酒馆主题 / 强制白天（白底黑字） / 强制夜间（黑底白字）。
- 修正 UNO 角色陪玩在摸牌后的行动约束，并统一使用同一套合法出牌判断。
